mutation ToggleActivate($adminId: ID!) {
  toggleActivate(adminId: $adminId) {
    errors {
      field
      message
    }
    admin {
      _id
      fullName
      fname
      lname
      email
      role
      adminId
      isActive
      createdAt
      updatedAt
    }
  }
}
