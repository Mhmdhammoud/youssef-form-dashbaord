mutation CreateAdmin($input: CreateAdminInput!) {
  createAdmin(input: $input) {
    errors {
      field
      message
    }
    admin {
      _id
      fullName
      fname
      lname
      email
      role
      adminId
      isActive
      createdAt
      updatedAt
    }
  }
}
